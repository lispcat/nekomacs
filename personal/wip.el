
;; kind-icon

;; corfu-terminal

;; corfu-candidate-overlay

;; nerd-icons-corfu


;; epub reader

;; (use-package calibredb)

;; org capture
(use-package-local org-capture
  :general
  (neko/leader-definer
    "oc" 'org-capture))
