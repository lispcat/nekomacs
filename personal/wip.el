
;; kind-icon

;; corfu-terminal

;; corfu-candidate-overlay

;; nerd-icons-corfu


;; epub reader

;; (use-package calibredb)

;; org capture
(use-package-local org-capture
  :config
  (mimi/leader-define-key
    "oc" 'org-capture))
